#ifndef TC_HASH_HUB_H
#define TC_HASH_HUB_H

/*
 * tc_hub_add() - add a harbor to a link
 * @extra_data: user_data	
 *
 * Return: 0 if successful, -1 if not
 */
int
tc_hash_hub_add(
	unsigned long user_data
);

#endif
